<a href="/profile"><button class="returnHome-btn" style="color: black; width: 10%;">Volver</button></a>

<h1>Administracion de Usuarios</h1>

<div class="container">
    <div class="cards-container">
        {{#each user}}
            <div class="users-card">
                <p>{{this.first_name}} {{this.last_name}}</p>
                <p class="email">{{this.email}}</p>
                <p>Rol: {{this.role}}</p>
                <div class="btns">
                    <button id="open-modal-role" class="role">Modificar rol</button>
                    <button id="open-modal-delete" class="delete">Eliminar</button>
                </div>
            </div>
        {{/each}}
    </div>
</div>

    <section class="modal">
        <div class="modal-container">
            <h2 id="modal-title" class="modal-title"></h2>
            <p id="modal-paragraph" class="modal-paragraph"></p>
            <div class="modal-btns">
                <button id="modal-close-role" class="modal-btn">Cerrar</button>
            </div>
        </div>
    </section>

    <section class="modal-delete">
        <div class="modal-container-delete">
            <h2 id="modal-title-delete" class="modal-title-delete">Hola</h2>
            <p id="modal-paragraph-delete" class="modal-paragraph-delete"></p>
            <div class="modal-btns-delete">
                <button id="delete-user-delete" class="modal-btn-delete">Eliminar</button>
                <button id="modal-close-delete" class="modal-close-btn-delete">Cerrar</button>
            </div>
        </div>
    </section>

<script src="/js/usersAdmin.js"></script>