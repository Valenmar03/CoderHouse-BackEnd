<div>

<button><a href="/">Volver a productos</a></button>
{{#if prod}}
    <h1>{{{prod.title}}}</h1>

    <P>{{{prod.description}}}</P>
    <P>Precio: ${{{prod.price}}}</P>

{{else}}
    <p>No se encontraron productos</p>
{{/if}}

<button>Agregar al Carrito</button>
    
    
    
</div>